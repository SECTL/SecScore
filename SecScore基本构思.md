# 积分管理软件开发指南

## 🎯 项目概述
这是一个为**教育场景**设计的本地桌面积分管理软件，主要用于**教师管理学生积分**，防止学生随意修改数据。

## 📋 核心功能
1. **积分管理** - 给学生加分/减分
2. **排行榜查询** - 查看积分排名
3. **密码保护** - 三级权限管理
4. **界面自定义** - 换主题和背景

## 🛠️ 技术方案

### 前端界面
```
React 18 + Fluent UI v9 + Vite
```
- 用React写界面
- 用微软的Fluent UI做美观组件
- 用Vite快速构建

### 桌面外壳
```
Electron
```
- 打包成Windows/Mac/Linux可执行文件
- 一次开发，多平台运行

### 数据存储
```
LowDB + JSON文件
```
- 数据存为本地JSON文件
- 简单易用，无需安装数据库

## 📁 三个主要界面

### 1. 积分管理界面
```
[界面布局]
姓名： [下拉选择学生]
分数： ○加分 ○扣分 [输入数字]
理由： [下拉选择理由] 或 [自定义理由]
[提交按钮]
```
- 下拉框选学生，支持搜索
- 加减分单选，输入数字
- 理由可预设，可自定义

### 2. 排行榜界面
```
[排行榜表格]
排名 | 姓名 | 总积分 | 今日变化 | 操作记录
  1  | 张三 |  150  |   +10   | [查看]
  2  | 李四 |  135  |    0    | [查看]
```
- 表格显示排名
- 可筛选时间（今天/本周/本月）
- 可导出Excel/CSV
- 点击"查看"看历史记录

### 3. 设置界面
```
[四个标签页]
外观 | 安全 | 数据管理 | 关于
```
- **外观**：换主题、换背景
- **安全**：设置密码权限
- **数据管理**：导入导出数据
- **关于**：软件信息

## 🔐 密码保护系统

### 三种权限
1. **管理密码** - 能改一切
2. **积分密码** - 只能改积分
3. **无密码** - 只能看，不能改

### 密码规则
- 纯6位数字密码
- 密码加密存储（AES加密）
- 忘记密码可用"找回字符串"重置

### 数据加密示例
```javascript
// 实际存储的密码是加密后的
{
  "adminPassword": "a1b2c3...加密字符串",
  "pointsPassword": "d4e5f6...加密字符串", 
  "recoveryString": "我爱吃苹果...加密字符串"
}
```

## 🎨 主题系统

### 三个预设主题
1. **浅色主题** - 默认白色
2. **深色主题** - 黑色背景
3. **护眼绿** - 绿色背景

### 自定义背景
- 可选纯色背景
- 可选本地图片背景
- 实时预览效果

## 💾 数据文件结构
```
用户数据目录/
├── database.json      # 主数据库
├── settings.json      # 所有设置
├── backups/           # 备份文件
└── logs/             # 日志文件
```

### 数据库结构
```json
{
  "users": [
    {"id": "001", "name": "张三", "points": 150}
  ],
  "transactions": [
    {"userId": "001", "points": 10, "reason": "回答问题", "time": "2024-01-01 09:00"}
  ],
  "reasons": ["回答问题", "作业优秀", "课堂表现", "其他"]
}
```

## 🚀 开发步骤

### 第一步：环境搭建（1天）
```bash
# 创建项目
npm create electron-app points-manager

# 安装核心依赖
npm install react react-dom
npm install @fluentui/react-components
npm install lowdb electron-store electron-log
```

### 第二步：基础框架（2-3天）
1. 创建Electron主窗口
2. 实现侧边栏导航
3. 搭建三个空白界面
4. 配置Vite构建

### 第三步：积分管理功能（3-4天）
1. 学生选择下拉框
2. 分数输入组件
3. 理由选择组件
4. 数据保存逻辑

### 第四步：排行榜功能（2-3天）
1. 表格组件
2. 时间筛选功能
3. 导出功能
4. 历史记录查看

### 第五步：设置功能（3-4天）
1. 外观设置（主题+背景）
2. 密码保护系统
3. 数据导入导出
4. 密码加密功能

### 第六步：测试打包（1-2天）
1. 测试所有功能
2. 修复bug
3. 打包成安装程序

## 🔧 关键代码示例

### 1. 积分管理组件
```jsx
function PointsManager() {
  const [selectedStudent, setSelectedStudent] = useState('');
  const [points, setPoints] = useState(0);
  const [reason, setReason] = useState('');
  
  const handleSubmit = async () => {
    // 检查权限
    if (!hasPermission('modify')) {
      await requestPassword('points');
    }
    
    // 保存记录
    await saveRecord({
      student: selectedStudent,
      points: points,
      reason: reason
    });
  };
  
  return (
    <div>
      <Dropdown label="选择学生" options={students} />
      <div>
        <Radio name="type" value="add" label="加分" />
        <Radio name="type" value="subtract" label="扣分" />
        <Input type="number" value={points} />
      </div>
      <Dropdown label="理由" options={reasons} />
      <Button onClick={handleSubmit}>提交</Button>
    </div>
  );
}
```

### 2. 密码管理器
```javascript
class PasswordManager {
  // 加密密码
  encrypt(password) {
    const iv = crypto.randomBytes(16);
    const cipher = crypto.createCipheriv('aes-256-cbc', secretKey, iv);
    return iv.toString('hex') + ':' + cipher.update(password, 'utf8', 'hex');
  }
  
  // 验证密码
  verify(input, storedEncrypted) {
    const parts = storedEncrypted.split(':');
    const iv = Buffer.from(parts[0], 'hex');
    const decipher = crypto.createDecipheriv('aes-256-cbc', secretKey, iv);
    const decrypted = decipher.update(parts[1], 'hex', 'utf8');
    return input === decrypted;
  }
}
```

## 📦 最终打包
```bash
# 开发模式
npm run dev

# 构建安装包
npm run build

# 生成Windows安装程序
npm run dist:win

# 生成Mac安装程序  
npm run dist:mac
```

## ⚠️ 注意事项

### 教育场景特殊需求
1. **简单易用** - 老师操作要简单
2. **防学生篡改** - 密码保护要有效
3. **数据安全** - 定期自动备份
4. **离线可用** - 不依赖网络

### 技术要点
1. 所有数据存本地
2. 密码加密存储
3. 支持数据导入导出
4. 定期自动备份

## 📈 扩展可能性

### 未来可加功能
1. **批量导入** - 从Excel导入学生名单
2. **统计图表** - 积分变化趋势图
3. **网络同步** - 多台电脑数据同步
4. **消息通知** - 积分变动通知

## 🎯 开发重点排序
1. ✅ 积分管理（核心功能）
2. ✅ 排行榜查看
3. ✅ 密码保护
4. ✅ 数据导入导出
5. ⏳ 主题自定义
6. ⏳ 自动备份
7. ⏳ 日志记录

## 💡 给AI助手的提示

### 当要求编写代码时：
1. 使用React函数组件
2. 使用Fluent UI组件
3. 数据操作通过IPC调用主进程
4. 密码相关操作在主进程完成

### 代码风格要求：
- 清晰的变量名
- 适当的注释
- 错误处理完善
- 类型安全（TypeScript可选）

### 文件结构建议：
```
src/
├── main/          # Electron主进程代码
├── renderer/      # React界面代码  
├── shared/        # 共享类型和工具
└── assets/        # 图片等资源
```

## 🏁 开始开发
按照上述指南，AI助手可以：
1. 先搭建Electron+React基础框架
2. 实现三个主要界面
3. 添加LowDB数据存储
4. 实现密码保护系统
5. 添加主题自定义功能
6. 测试并打包

这个软件适合教师管理课堂积分，功能实用，实现难度中等，预计2-3周可完成基本版本。